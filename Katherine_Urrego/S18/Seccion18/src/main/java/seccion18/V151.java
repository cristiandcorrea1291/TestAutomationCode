package seccion18;

public class V151 {
		
	
	//video 151. Create new Maven Project and all Framework dependencies
	/*
	 * 1. crear un projecto tipo maven
	 * file -> new -> maven project
	 * seleccionar archetype -> maven-archetype-quickstart
	 * Group id-- > nombre de la compalia ( es una herarquia mas alta que el artefacto group Id means it's like hierarchy upper hierarchy to artifact.
	 * Artifact Id -> es el project name
	 * Package -> podemos dejar el nombre por defecto o cambiarlo
	 * clic en finish
	 * 
	 * Esperar a que se cree el poryecto
	 * 
	 * 
	 * En el archivo pom,xml se puden:
	 * 
	 * ver la información del proyecto grpp id, artifac y version ...
	 *  esta información sirve  if you want to publish this project to Maven Repository, you can do that with these three simple details.
	 *  
	 *  Vamos al maven repository y traemos losjars de selenium web driver, testng..
	 *  
	 *  
	 *  el maven project tiene la capacidad de interacutar con el maven repository y obtener todos los JARS necesarios  a traves de las "dependencies" para generar la automatización
	 */
}
